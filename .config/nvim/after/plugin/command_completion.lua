---------------------------------------
-- Nvim commands completion settings --
--------------------------------------

require('command-completion').setup({
    border = 'single',
    max_col_num = 5,
    min_col_width = 5,
    tab_completion = true,
})
